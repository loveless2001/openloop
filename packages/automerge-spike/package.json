{
  "name": "@openloop/automerge-spike",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@automerge/automerge": "^3.2.6"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}
